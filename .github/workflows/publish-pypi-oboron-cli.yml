name: Publish oboron-cli to PyPI

# Triggers on `oboron-cli-py/v*` tag pushes to this GitHub mirror.
# Builds the full wheel matrix (5 platforms + sdist) for the
# `oboron-cli-py/` package and publishes atomically to PyPI as
# `oboron-cli` via OIDC trusted publishing — no API token needed on
# the GitHub side.
#
# The wheel is bin-only: it ships the `ob` and `obz` Rust binaries
# from the sibling `oboron-cli/` crate as a wheel scripts payload
# (maturin packages every `[[bin]]` target, so a single wheel
# delivers both). The pyproject.toml at `oboron-cli-py/pyproject.toml`
# points maturin at `../oboron-cli/Cargo.toml` via `[tool.maturin]
# manifest-path`; the workflow just `cd`s into `oboron-cli-py/` and
# lets maturin pick that up.
#
# One-time setup required before the first publish:
#
# 1. On PyPI (https://pypi.org/manage/project/oboron-cli/settings/publishing/):
#    Add a Trusted Publisher with:
#      Owner:        deyanovich
#      Repository:   oboron-tools-rs
#      Workflow:     publish-pypi-oboron-cli.yml
#      Environment:  pypi
#
#    Note: the `oboron-cli` PyPI project already exists (releases
#    0.1.0 / 0.2.0 / 0.3.0 from the predecessor oboron-rs
#    workspace). Trusted Publisher must be added to the existing
#    project rather than creating a new one.
#
# 2. On GitHub (Settings → Environments on this repo):
#    Create an environment named `pypi` if not already present.
#    Optional protection rules (e.g. require approval for
#    deployment) can be added there.
#
# Adapted from the working obcrypt-rs publish workflow
# (github.com/deyanovich/obcrypt-rs) — same matrix targets and OIDC
# trusted-publisher pattern that proved out in production for
# oboron-py and obcrypt-py.

on:
  push:
    tags:
      - "oboron-cli-py/v*"

permissions:
  contents: read

jobs:
  build-wheels:
    name: Build wheels (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-14
            target: aarch64-apple-darwin
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          working-directory: oboron-cli-py
          target: ${{ matrix.target }}
          args: >-
            --release
            --strip
            --out dist
          manylinux: auto
          python-version: "3.12"

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.target }}
          path: oboron-cli-py/dist/*.whl

  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          working-directory: oboron-cli-py
          command: sdist
          args: --out dist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: oboron-cli-py/dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build-wheels, build-sdist]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect dist files
        run: |
          mkdir dist
          find artifacts -type f \( -name "*.whl" -o -name "*.tar.gz" \) -exec cp {} dist/ \;
          ls -la dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
